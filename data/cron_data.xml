<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="ir_cron_bi_sync" model="ir.cron">
            <field name="name">Generative BI: Sync to BigQuery</field>
            <field name="model_id" ref="model_bi_export_config"/>
            <field name="state">code</field>
            <field name="code">model.run_scheduler()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <!-- numbercall removed as it is invalid in Odoo 19 -->
            <field name="active" eval="False"/>
        </record>
    </data>
</odoo>
